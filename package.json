{
  "name": "@stencila/status",
  "description": "Stencila's status reporting",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "statusfy dev",
    "build": "statusfy build",
    "start": "statusfy start",
    "generate": "statusfy generate",
    "new-incident": "statusfy new-incident",
    "delete-incident": "statusfy delete-incident",
    "update-incident": "statusfy update-incident",
    "deploy": "gh-pages -d dist",
    "publish": "npm run generate && npm run deploy"
  },
  "dependencies": {
    "@babel/preset-env": "^7.12.13",
    "statusfy": "^0.5.0"
  },
  "devDependencies": {
    "@stencila/dev-config": "2.0.7",
    "gh-pages": "3.1.0"
  },
  "renovate": {
    "extends": [
      "@stencila"
    ]
  }
}
